package fr.inria.jessy.utils;

public class Compress {

	/*
	 * PIERRE
	 * I comment this out cause the pattern to access an object using a key 
	 * is rather unclear (for instance, should I compress the name first ?) , 
	 * and not homogeneous across benchmarks.
	 * 
	 */
	public static String compressClassName(String className) { 
		return className;
	}

}